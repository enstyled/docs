---
title: 'Introduction'
description: 'The JobKit REST API allows you to integrate your job board with external services, automate tasks and create custom workflows.'
icon: 'map'
---

All endpoints are accessible on your job board web address and accepts JSON or form-encoded request and returns JSON-encoded responses. The API uses standard HTTP verbs, response codes and authentication.
