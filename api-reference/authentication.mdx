---
title: 'Authentication'
description: 'All API endpoints are accessible on your job board address and accepts JSON or form-encoded request bodies, returns JSON-encoded responses, and uses standard HTTP verbs, response codes and authentication.'
icon: 'at'
---

```json
"security": [
  {
    "bearerAuth": []
  }
]
```
