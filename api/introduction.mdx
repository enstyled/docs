---
title: 'Introduction'
description: 'The API allows to integrate your job board with other services, automate tasks and create custom workflows.'
icon: 'map'
---

All endpoints are accessible on your job board address and accepts JSON or form-encoded request and returns JSON-encoded responses. The API uses standard HTTP verbs, response codes and authentication.
